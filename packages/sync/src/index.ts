/**
 * Offline sync engine (outbox, delta apply, conflict rules) — implementation begins Phase 1
 * Phase 0 stub — exists so the workspace graph and CI pipelines are real
 * from day one (phase-0-foundations.md §0.1).
 */
export const PACKAGE_NAME = '@retailos/sync';
