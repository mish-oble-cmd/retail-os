# Copy to .env for local dev (never commit .env — see security-and-compliance.md §Secrets)
DATABASE_URL=postgres://retailos:retailos@localhost:5433/retailos
SESSION_SECRET=change-me
PORT=3001
LOG_LEVEL=info
# SENTRY_DSN=            # pending credentials
